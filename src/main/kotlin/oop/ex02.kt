package oop
fun main(args: Array<String>) {
//Control flows allows you do something conditionally
//and repeatedly run code (conditionally)
    var age = 17
    if(age>=21)
    println("you may vote now")

    var weight = "68kg"
    if (weight<="68kg")
        println("You are in lightweight division")

    var temp = 150
    if (temp>=150)
        println("Cake is likely to burn")



}

